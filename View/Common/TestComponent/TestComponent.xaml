<UserControl x:Class="KH_Editor.View.Common.TestComponent.TestComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel LastChildFill="False" Background="Black">

        <!-- STYLE -->
        <DockPanel.Resources>
            <Style TargetType="Label">
                <Setter Property="Foreground" Value="White"/>
            </Style>
        </DockPanel.Resources>

        <Label DockPanel.Dock="Top" Height="30">GAME:</Label>
        <Label DockPanel.Dock="Top" Height="30">Connected</Label>
        <Frame DockPanel.Dock="Left"/>
        <Grid DockPanel.Dock="Bottom" Height="100">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Click="readAction">Read</Button>
            <Button Grid.Row="1" Click="writeAction">Write</Button>
        </Grid>
    </DockPanel>
    
</UserControl>
